(function () {
    "use strict";
    const SETTINGS_KEY = "pengaturanAplikasi";
    const THEME_KEY = "themeMode";
    const LOGO_KEY = "logoDashboard";
    const DEFAULT_LOGO_FILE = "logo-catatan-kas.jpg";
    const FALLBACK_LOGO_FILE = "icon-192.png";
    function accountKey(base) { const uid = String(window.currentFirebaseUid || window.currentFirebaseUser?.uid || "").trim(); return uid ? `${base}_${uid}` : base; }
    function getSettings() { try { const data = JSON.parse(localStorage.getItem(accountKey(SETTINGS_KEY)) || "{}"); return data && typeof data === "object" ? data : {}; } catch (_) { return {}; } }
    function saveSettings(settings) { try { localStorage.setItem(accountKey(SETTINGS_KEY), JSON.stringify(settings)); } catch (_) {} }
    function getSavedTheme() { const settings = getSettings(); if (["dark","light","system"].includes(settings.tema)) return settings.tema; try { const saved = localStorage.getItem(accountKey(THEME_KEY)); if (["dark","light","system"].includes(saved)) return saved; } catch (_) {} return "light"; }
    function resolveTheme(theme) { if (theme === "system") { try { return window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light"; } catch (_) { return "light"; } } return theme === "dark" ? "dark" : "light"; }
    function applyTheme(theme, simpan = true) { if (!["dark","light","system"].includes(theme)) theme = "light"; const resolvedTheme = resolveTheme(theme); const isDark = resolvedTheme === "dark"; document.documentElement.setAttribute("data-theme", resolvedTheme); document.documentElement.classList.toggle("dark-mode", isDark); document.body?.classList.toggle("dark-mode", isDark); const metaTheme = document.querySelector('meta[name="theme-color"]'); if (metaTheme) metaTheme.setAttribute("content", isDark ? "#121212" : "#198754"); if (simpan) { try { localStorage.setItem(accountKey(THEME_KEY), theme); } catch (_) {} const settings = getSettings(); settings.tema = theme; saveSettings(settings); } window.dispatchEvent(new CustomEvent("themeChanged", { detail: { theme, resolvedTheme, dark: isDark } })); }
    function absoluteFile(file) { try { return new URL(file, document.baseURI).href; } catch (_) { return file; } }
    function normalizeLogo(value) { if (!value || typeof value !== "string") return null; const logo = value.trim(); if (!logo) return null; if (logo === "assets/logo-catatan-kas.jpg" || logo === "assets/logo-catatan-kas.png" || logo === "logo-catatan-kas.png") return null; if (/^data:image\//i.test(logo)) return logo; if (/^blob:/i.test(logo)) return logo; if (/^(https?:\/\/|\.\/|\/|[\w.-]+\/)/i.test(logo)) return logo; if (/^[\w.-]+\.(jpe?g|png|webp|gif|svg)$/i.test(logo)) return logo; return null; }
    function getLogo() { const settings = getSettings(); let saved = null; try { saved = localStorage.getItem(accountKey(LOGO_KEY)); } catch (_) {} for (const candidate of [saved, settings.logoDashboard, settings.logo]) { const normalized = normalizeLogo(candidate); if (normalized) return normalized; } return absoluteFile(DEFAULT_LOGO_FILE); }
    function applyOneLogo(img, logo) { if (!img || img.tagName !== "IMG") return; img.removeAttribute("srcset"); img.removeAttribute("data-src"); img.setAttribute("data-catatan-kas-logo", "true"); if (!img.dataset.logoFallbackBound) { img.dataset.logoFallbackBound = "true"; img.addEventListener("error", function () { const fallback = absoluteFile(FALLBACK_LOGO_FILE); if (img.getAttribute("src") !== fallback) img.src = fallback; }); } if (img.getAttribute("src") !== logo) { img.removeAttribute("data-logo-sync-applied"); img.src = logo; } }
    let applyingLogo = false;
    function applyLogo() { if (applyingLogo) return; applyingLogo = true; try { const logo = getLogo(); const selector = [".app-logo", ".ck-logo", ".logo", ".top .logo", ".topbar .logo", "#logoDashboard", "#dashboardLogo", "#logoPreviewV2", "#logoPreview", "#previewLogoDashboard", "#laporanLogo", "#logo", "img[alt='Logo Dashboard']", "img[alt='Logo aplikasi']", "img[alt='Logo Catatan Kas']", "[data-dashboard-logo]"].join(","); document.querySelectorAll(selector).forEach(img => applyOneLogo(img, logo)); } finally { applyingLogo = false; } }
    function setupLogoSync() { applyLogo(); window.addEventListener("logoDashboardChanged", applyLogo); window.addEventListener("storage", event => { if (event.key === LOGO_KEY || event.key === SETTINGS_KEY || event.key?.startsWith(`${LOGO_KEY}_`) || event.key?.startsWith(`${SETTINGS_KEY}_`)) applyLogo(); }); window.addEventListener("pageshow", applyLogo); window.addEventListener("focus", applyLogo); if (window.MutationObserver) { let queued = false; const observer = new MutationObserver(mutations => { const relevant = mutations.some(m => { if (m.type === "childList") return true; if (m.type === "attributes" && m.attributeName === "src") return m.target?.matches?.(".app-logo, .ck-logo, .logo, .top .logo, .topbar .logo, #logoDashboard, #dashboardLogo, #laporanLogo, #logo, img[alt='Logo Dashboard'], img[alt='Logo aplikasi'], img[alt='Logo Catatan Kas'], [data-dashboard-logo]"); return false; }); if (!relevant || queued) return; queued = true; requestAnimationFrame(() => { queued = false; applyLogo(); }); }); observer.observe(document.documentElement, { childList: true, subtree: true, attributes: true, attributeFilter: ["src"] }); } setTimeout(applyLogo, 0); setTimeout(applyLogo, 250); setTimeout(applyLogo, 1000); }
    function toggleTheme() { const current = document.documentElement.classList.contains("dark-mode"); applyTheme(current ? "light" : "dark", true); }
    function setupThemeToggle() { document.addEventListener("click", event => { const tombol = event.target.closest("#themeToggle, [data-theme-toggle]"); if (!tombol) return; event.preventDefault(); toggleTheme(); }); }
    function setupSystemThemeListener() { if (!window.matchMedia) return; const media = window.matchMedia("(prefers-color-scheme: dark)"); const handleChange = () => { if (getSavedTheme() === "system") applyTheme("system", false); }; if (typeof media.addEventListener === "function") media.addEventListener("change", handleChange); else if (typeof media.addListener === "function") media.addListener(handleChange); }
    function startThemeManager() { applyTheme(getSavedTheme(), false); setupThemeToggle(); setupSystemThemeListener(); setupLogoSync(); }
    if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", startThemeManager, { once: true }); else startThemeManager();
    window.themeManager = { getTheme: getSavedTheme, setTheme: applyTheme, toggle: toggleTheme, init: startThemeManager, resolve: resolveTheme, getLogo, applyLogo };
    function loadSantriImport() { if (!document.getElementById("tabelSantri") || document.getElementById("importSantriLoader")) return; const s = document.createElement("script"); s.id = "importSantriLoader"; s.type = "module"; s.src = "js/santri-import.js"; document.body.appendChild(s); }
    if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", loadSantriImport, { once: true }); else loadSantriImport();
    import("./js/jenis-keuangan.js").catch(() => {});
    function loadReportActions() { if (!/laporan\.html$/i.test(location.pathname) || window.__ckReportActionsLoaded) return; window.__ckReportActionsLoaded = true; import("./laporan-actions.js").catch(err => console.error("Gagal memuat aksi laporan:", err)); }
    if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", loadReportActions, { once: true }); else loadReportActions();
    function loadExcelExport() { if (!/dashboard-excel\.html$/i.test(location.pathname) || window.__ckExcelExportLoaded) return; window.__ckExcelExportLoaded = true; import("./js/excel-export.js").catch(err => console.error("Gagal memuat Export Excel:", err)); }
    if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", loadExcelExport, { once: true }); else loadExcelExport();
    import("./firebase-config.js").then(({auth}) => import("https://www.gstatic.com/firebasejs/12.17.1/firebase-auth.js").then(({onAuthStateChanged}) => { onAuthStateChanged(auth, user => { window.currentFirebaseUid = user?.uid || ""; window.currentFirebaseUser = user || null; applyTheme(getSavedTheme(), false); applyLogo(); window.dispatchEvent(new CustomEvent("accountDataReady", { detail: { uid: user?.uid || null } })); } })).catch(() => {});
})();
